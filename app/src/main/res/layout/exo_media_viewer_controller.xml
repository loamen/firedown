<?xml version="1.0" encoding="utf-8"?>
<!--
  Controller layout for PlayerView. Used via app:controller_layout_id
  on @id/player_view in fragment_media_viewer.xml.

  PlayerView is hidden by default at launch (autoShow=false in
  MediaViewerFragment); a user tap brings it up. The MediaViewerFragment
  attaches an OnApplyWindowInsetsListener to media_viewer_controller_root
  to set bottom padding equal to the navigation-bar inset whenever the
  system bars are visible — so this view is always above the nav bar
  while the video TextureView behind it stays edge-to-edge.

  The view IDs (exo_progress, exo_play_pause, exo_position, exo_duration,
  exo_prev, exo_next, exo_rew_with_amount, exo_ffwd_with_amount) are
  required by Media3's PlayerControlView reflection lookup — do not
  rename them.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/media_viewer_controller_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:background="@drawable/bg_media_viewer_controller"
    android:orientation="vertical"
    android:paddingTop="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageButton
            android:id="@id/exo_prev"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/previous"
            android:scaleType="centerInside"
            android:src="@drawable/ic_media_skip_previous_white_24" />

        <TextView
            android:id="@id/exo_rew_with_amount"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="12dp"
            android:background="@drawable/ic_media_replay_white_24"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="10sp" />

        <ImageButton
            android:id="@id/exo_play_pause"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/play"
            android:scaleType="centerInside"
            android:src="@drawable/media_action_play" />

        <TextView
            android:id="@id/exo_ffwd_with_amount"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/ic_media_fast_forward_white_24"
            android:fontFamily="sans-serif-medium"
            android:gravity="center"
            android:textColor="@android:color/white"
            android:textSize="10sp" />

        <ImageButton
            android:id="@id/exo_next"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/next"
            android:scaleType="centerInside"
            android:src="@drawable/ic_media_skip_next_white_24" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp">

        <TextView
            android:id="@id/exo_position"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textColor="@android:color/white"
            android:textSize="12sp" />

        <androidx.media3.ui.DefaultTimeBar
            android:id="@id/exo_progress"
            android:layout_width="0dp"
            android:layout_height="28dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1" />

        <TextView
            android:id="@id/exo_duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textColor="@android:color/white"
            android:textSize="12sp" />
    </LinearLayout>
</LinearLayout>
