<?xml version="1.0" encoding="utf-8"?>
<!--
  Controller layout for PlayerView. Used via app:controller_layout_id
  on @id/player_view in fragment_media_viewer.xml.

  PlayerView is hidden by default at launch (autoShow=false in
  MediaViewerFragment); a user tap brings it up. The fragment attaches
  an OnApplyWindowInsetsListener to media_viewer_controller_root so
  bottom padding matches the navigation-bar inset whenever the system
  bars are visible — the view stays above the nav bar while the video
  TextureView behind it remains edge-to-edge.

  Three rows:
    1. Utility row (top-right): PiP, rotate. App-defined IDs; the
       fragment wires their click handlers to PlayerActivity.
    2. Playback row (centre): prev, play/pause, next. Standard Media3
       view IDs (exo_prev / exo_play_pause / exo_next) so Media3
       binds playback behaviour.
    3. Scrubber row: exo_position, exo_progress (DefaultTimeBar in
       brand orange), exo_duration. Also Media3-managed.

  Do not rename the exo_* IDs — Media3 looks them up by name.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/media_viewer_controller_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:background="@drawable/bg_media_viewer_controller"
    android:orientation="vertical"
    android:paddingTop="8dp">

    <!-- Utility row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end|center_vertical"
        android:orientation="horizontal"
        android:paddingStart="8dp"
        android:paddingEnd="8dp">

        <ImageButton
            android:id="@+id/media_viewer_btn_rotate"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/rotate"
            android:scaleType="centerInside"
            android:src="@drawable/ic_media_screen_rotation_white_24" />

        <ImageButton
            android:id="@+id/media_viewer_btn_pip"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="4dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/picture_in_picture"
            android:scaleType="centerInside"
            android:src="@drawable/ic_media_pip_white_24" />
    </LinearLayout>

    <!-- Playback row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingTop="8dp"
        android:paddingBottom="4dp">

        <ImageButton
            android:id="@id/exo_prev"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/previous"
            android:scaleType="centerInside"
            android:src="@drawable/ic_media_skip_previous_white_24" />

        <ImageButton
            android:id="@id/exo_play_pause"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/play"
            android:scaleType="centerInside"
            android:src="@drawable/media_action_play" />

        <ImageButton
            android:id="@id/exo_next"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/next"
            android:scaleType="centerInside"
            android:src="@drawable/ic_media_skip_next_white_24" />
    </LinearLayout>

    <!-- Scrubber row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="16dp"
        android:paddingTop="8dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp">

        <TextView
            android:id="@id/exo_position"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textColor="@android:color/white"
            android:textSize="12sp" />

        <androidx.media3.ui.DefaultTimeBar
            android:id="@id/exo_progress"
            android:layout_width="0dp"
            android:layout_height="28dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_weight="1"
            app:played_color="@color/brand_orange"
            app:scrubber_color="@color/brand_orange" />

        <TextView
            android:id="@id/exo_duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:textColor="@android:color/white"
            android:textSize="12sp" />
    </LinearLayout>
</LinearLayout>
